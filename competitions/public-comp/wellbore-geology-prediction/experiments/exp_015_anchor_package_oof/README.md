# EXP-015 — Anchor × package OOF

Research-only Kaggle notebook. It aligns production residual-anchor OOF with model-package OOF and performs five-fold well-grouped meta-validation of a disagreement-gated residual.

It never writes `submission.csv` and should not be submitted to the competition.

Required inputs are listed in `kernel-metadata.json`.
