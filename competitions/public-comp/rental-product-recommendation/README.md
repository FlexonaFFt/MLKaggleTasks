# Rental Product Recommendation

Preprocessing notebook for rental product recommendation data.

## Status

Notebook preprocessing pipeline.

## What to Review

- `pipeline.ipynb` - data preparation workflow.

## Data

Expected local data is stored in `datasets/` and derived data in `processed/`.
Both are ignored by git.
